interface:
  display_name: "Analyze GPU Workload"
  short_description: "Summarize real HP and Spot trace pressure"
  default_prompt: "Use $analyze-gpu-workload to analyze a real Alibaba GPU trace window."
