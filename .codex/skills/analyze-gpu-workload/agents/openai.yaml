interface:
  display_name: "Analyze GPU Workload"
  short_description: "Summarize any canonical GPU trace window"
  default_prompt: "Use $analyze-gpu-workload to build a fingerprinted workload summary for a canonical Trace."
